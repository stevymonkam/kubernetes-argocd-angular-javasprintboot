apiVersion: v1
kind: Service
metadata:
  name: mysqldb
  labels:
    app: mysqldb
    environment: prod
spec:
  ports:
    - port: 3307
      targetPort: 3306
      protocol: TCP
      name: mysql
