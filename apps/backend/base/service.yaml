apiVersion: v1
kind: Service
metadata:
  namespace: dev
  name: backend
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: 8086
      targetPort: 8086
  selector:
    app: backend
